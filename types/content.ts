export type NavigationItem = {
  label: string;
  href: string;
};

export type OutcomeItem = {
  number: string;
  title: string;
  description: string;
};

export type ServiceItem = {
  title: string;
  description: string;
};

export type ContactConfig = {
  contactName: string;
  displayPhone: string;
  phoneHref: string;
  email: string;
};
