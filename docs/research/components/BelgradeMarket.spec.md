# BelgradeMarket specification

- Target: `components/BelgradeMarket.tsx`
- Interaction: reveal; decorative marker pulse disabled for reduced motion.
- Background `#171816`; min-height 760px; two columns.
- Left heading clamp(42px, 5vw, 68px) and three principle rows.
- Right: inline SVG line map inspired by Belgrade river/urban contours, warm taupe paths at low opacity and four neutral markers.
- Principles: dynamic prices, availability management, review/demand monitoring.
- SVG is decorative with `aria-hidden=true`; mobile places it below copy at 360px height.
