# FinalCTA specification

- Target: `components/FinalCTA.tsx`
- Interaction: reveal, image hover, phone link.
- Light section with `/images/villa-dusk.jpg`, 680px desktop / 560px mobile.
- Dark panel overlaps lower-left 58% of image and uses a large concave circular right edge via border-radius/clip-path.
- Heading clamp(42px, 5vw, 70px), max-width 620px; body max 560px; light pill CTA.
- Mobile panel becomes a normal block with a rounded top-right corner and no overlap.
